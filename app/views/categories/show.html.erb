<p style="color: green"><%= notice %></p>

<%= render @category %>

<div>
  <%= link_to "Edit this category", edit_category_path(@category), class:"btn btn-outline-info" %> 
  <%= link_to "Back to categories", categories_path, class:"btn btn-outline-info" %>
<br/><br/>
  <%= button_to "Destroy this category", @category, method: :delete, class:"btn btn-outline-secondary" %>
</div>
